
<%= form_for(@project) do |f| %>
 <h3>Add Project</h3>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, :autofocus => true, class: 'form-control' %>
  </div>
  <%= f.submit 'Add Project', :class => 'button right' %>
<% end %>
